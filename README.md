# sql

Day 1: SELECT / WHERE / ORDER / LIMIT / DISTINCT / NULL

Key points:

NULL checks use IS NULL / IS NOT NULL (not = NULL)
BETWEEN is inclusive
DISTINCT works on selected columns
ORDER BY default is ASC
